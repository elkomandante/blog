<section class="cta-section theme-bg-light py-5">
    <div class="container text-center">
        <h2 class="heading">{{ infoTitle }}</h2>
        {% if infoIntro is defined %}
            <div class="intro">{{ infoIntro }}</div>
        {% endif %}
    </div><!--//container-->
</section>